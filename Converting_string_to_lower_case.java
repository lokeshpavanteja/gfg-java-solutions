class Solution {
    static String toLower(String s) {
        s.toLowerCase();
        return s.toLowerCase(); /*
                                 * s.toLowerCase is used to convert
                                 * the uppercase letter
                                 * into the lower case
                                 */
    }
}